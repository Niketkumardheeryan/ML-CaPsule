# Model Artifacts Folder

Run `mental_health_analysis.ipynb` to generate the trained model files in this folder.

Expected generated files:

```text
models/mental_health_tfidf_model.joblib
models/risk_thresholds.joblib
```

These generated files are intentionally not included by default because they can be recreated from the notebook.
