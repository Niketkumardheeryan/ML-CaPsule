# Movie recommendation system

This movie recommendation system uses collaborative filtering to recommend movies to existing users.

- Install requirements:

```bash
pip install -r reguirements.txt
```
else 
```bash
pip install lightfm-next kagglehub numpy pandas
```
- pipeline.ipynb \ This notebook implements training of movie recommendation system.

For more clarity on [collaborative filtering](https://developers.google.com/machine-learning/recommendation/collaborative/basics).